<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.schedule.app.repository.ScheduleCreateMapper">
    
    <insert id="createDefaultSchedule">
        INSERT INTO default_schedule (
            user_id,
            start_time,
            end_time,
            start_date,
            end_date,
            work_type_id
        ) VALUES (
            #{userId},
            #{startTime},
            #{endTime},
            #{startDate},
            #{endDate},
            #{workTypeId}
        )
    </insert>

    <insert id="createRegularSchedule">
        INSERT INTO regular_schedule (
            user_id,
            start_time,
            end_time,
            start_date,
            end_date,
            days_of_week,
            interval_weeks,
            work_type_id
        ) VALUES (
            #{userId},
            #{startTime},
            #{endTime},
            #{startDate},
            #{endDate},
            #{daysOfWeek},
            #{intervalWeeks},
            #{workTypeId}
        )
    </insert>

    <insert id="createIrregularSchedule">
        INSERT INTO irregular_schedule (
            user_id,
            date,
            start_time,
            end_time,
            work_type_id
        ) VALUES (
            #{userId},
            #{date},
            #{startTime},
            #{endTime},
            #{workTypeId}
        )
    </insert>
</mapper>